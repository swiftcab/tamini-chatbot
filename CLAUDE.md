# CLAUDE.md - AI Assistant Guide for tamini-chatbot

## Project Overview

**tamini-chatbot** is a chatbot application. This repository is in its initial setup phase.

## Repository Status

This project is newly initialized and does not yet contain application code. The sections below should be updated as the codebase evolves.

## Project Structure

```
tamini-chatbot/
├── CLAUDE.md          # This file - AI assistant guide
└── (empty)            # No source code yet
```

> **Note:** Update this section as directories and files are added.

## Development Workflow

### Getting Started

1. Clone the repository
2. Install dependencies (tool/package manager TBD)
3. Set up environment variables (if applicable)
4. Run the development server

### Branch Conventions

- Feature branches should use descriptive names
- Keep commits focused and atomic
- Write clear commit messages describing the "why"

## Conventions

### Code Style

- Follow consistent formatting (configure a linter/formatter early)
- Use clear, descriptive naming for variables, functions, and files
- Keep functions small and focused on a single responsibility

### Security

- Never commit secrets, API keys, or credentials
- Use environment variables for sensitive configuration
- Validate and sanitize all user inputs (especially important for a chatbot)

## Key Commands

> **Note:** Fill in actual commands once the tech stack is chosen.

```bash
# Install dependencies
# npm install / pip install -r requirements.txt / etc.

# Run development server
# npm run dev / python main.py / etc.

# Run tests
# npm test / pytest / etc.

# Lint code
# npm run lint / flake8 / etc.

# Build for production
# npm run build / etc.
```

## Architecture

> **Note:** Document the architecture here once implemented. Include:
> - Frontend framework (if any)
> - Backend framework
> - Database / data storage
> - Chat/messaging protocol
> - AI/NLP integration details
> - Authentication approach
> - Deployment target

## Environment Variables

> **Note:** Document required environment variables here (without actual values).

```
# Example:
# API_KEY=           # API key for chat service
# DATABASE_URL=      # Database connection string
# PORT=              # Server port
```

## Testing

> **Note:** Document testing strategy once established.

## Deployment

> **Note:** Document deployment process once configured.
